from setuptools import setup

setup(
    name="lru_dict",
    version='0.0.1',
    author='Alec Nikolas Reiter',
    description='Least Recently Used cache dict',
    licenses='MIT',
    packages=['lru_dict']
    )
